# Configuration file listing the superclasses and the packages in which to
# look for derived classes.
#
# Format:
# class.name=package1,package2,...
#
# Author:  FracPete (fracpete at waikato dot ac dot nz)
# Version: $Revision$

# the hadoop mappers
org.apache.hadoop.mapreduce.Mapper=\
  adams.data.hadoop.map

# the hadoop reducers
org.apache.hadoop.mapreduce.Reducer=\
  adams.data.hadoop.reduce

# the hadoop main classes
org.apache.hadoop.conf.Configured=\
  adams.data.hadoop.main
