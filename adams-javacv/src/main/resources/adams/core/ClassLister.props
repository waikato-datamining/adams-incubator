# Configuration file listing the superclasses and the packages in which to
# look for derived classes.
#
# Format:
# class.name=package1,package2,...
#
# Author:  FracPete (fracpete at waikato dot ac dot nz)
# Version: $Revision: 10248 $

# the BufferedImage feature generators
adams.data.image.features.AbstractBufferedImageFeatureGenerator=\
  adams.data.opencv.features

# the OpenCV feature generators
adams.data.opencv.features.AbstractOpenCVFeatureGenerator=\
  adams.data.opencv.features

# the OpenCV transformers
adams.data.opencv.transformer.AbstractOpenCVTransformer=\
  adams.data.opencv.transformer
